
# netlify.toml
[build]
  command = "npm run build"
  publish = "dist"

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Add environment variables placeholders for deployment
[template.environment]
  AUTH0_DOMAIN = "Your Auth0 Domain (optional)"
  AUTH0_CLIENT_ID = "Your Auth0 Client ID (optional)"
